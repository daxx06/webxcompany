deploy-pages:
  stage: deploy
  script:
    - echo "Deploying WebX site to GitLab Pages"
  pages: true
  environment: production
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
